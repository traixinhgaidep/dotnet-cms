@model List<Models.EF.Channel>

<div class="logo">
    <a href="/Frontend/Home/Index/"><img src="/Assets/Frontend/images/logo.png" class="img-responsive" alt="" /></a>
</div>
<label></label>
<h4 class="menn">Danh mục</h4>
<div class="head-nav">
    <span class="menu"> </span>
    @foreach (var item in Model)
    {
        var url = "/Frontend/Channel/ListArticleByChannel/" + item.IDChannel;
        <h4><a href="@url"><i class="fa fa-link"></i> <span>@item.Name</span></a></h4>
    }
</div>
<div class="clearfix"> </div>
